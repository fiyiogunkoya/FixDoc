terraform {
  required_version = ">= 1.5.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0"
    }
  }
}

provider "aws" {
  region = var.aws_region
  # Credentials from environment: AWS_PROFILE, AWS_ACCESS_KEY_ID, etc.
}
